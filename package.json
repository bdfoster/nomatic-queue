{
  "name": "nomatic-queue",
  "version": "0.0.0",
  "description": "Collection of queues, written with TypeScript.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "compile": "tsc",
    "test": "cross-env NODE_ENV=test nyc mocha",
    "coverage": "nyc report --reporter=html",
    "coveralls": "nyc report --reporter=text-lcov | coveralls",
    "docs": "typedoc --out ./docs/ --module commonjs --name nomatic-queue --hideGenerator --mode file --excludeExternals --excludePrivate ./src"
  },
  "directories": {
    "test": "test"
  },
  "author": "Brian D. Foster <bdfoster89@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^3.5.2",
    "@types/mocha": "^2.2.41",
    "chai": "^3.5.0",
    "cross-env": "^4.0.0",
    "mocha": "^3.3.0",
    "nyc": "^10.3.0",
    "source-map-support": "^0.4.15",
    "ts-node": "^3.0.2",
    "typescript": "^2.3.2"
  },
  "dependencies": {
    "lodash.get": "^4.4.2"
  }
}
